target-version = "py312"
line-length = 100
exclude = ["lab", ".venv", "migrations"]

[lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]  # line length handled by formatter

[lint.per-file-ignores]
"simulator/tests/*" = ["F401", "E402"]
